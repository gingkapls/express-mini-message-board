<html lang="en">
  <body>
    <h1><%= title %></h1>
    <ul>
      <% messages.forEach(({user, text, added, id}) => { %> <%-
      include('message', {user, text, added, id, formatDate}); %> <% }) %>
    </ul>
    <a href="/new">New message</a>
    <a href="/">Go home</a>
  </body>
</html>
